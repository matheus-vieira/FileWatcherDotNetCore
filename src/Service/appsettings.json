{
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    },
    "Console": {
      "LogLevel": {
        "Default": "Debug"
      }
    },
    "GELF": {
      "Host": "localhost",
      "Port": 12201, // Not required if using default 12201.
      "LogSource": "Kankei.FileWatcher", // Required if not set in code.
      "AdditionalFields": { // Optional fields added to all logs.
        "project_name": "Kankei.FileWatcher"
      },
      "LogLevel": {
        "Default": "Debug"
      }
    }
  }
}
